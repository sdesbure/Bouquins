#sub-nav
  %nav.sub-nav{:role => "navigation"}
    %ul
      - if request.env['REQUEST_PATH'] == user_read_books_path(current_user)
        %li{:class => "current"}= link_to "Mes livres lus", user_read_books_path(current_user)
      - else
        %li= link_to "Mes livres lus", user_read_books_path(current_user)
      - if request.env['REQUEST_PATH'] == user_owned_books_path(current_user)
        %li{:class => "current"}= link_to "Mes livres", user_owned_books_path(current_user)
      - else
        %li= link_to "Mes Livres", user_owned_books_path(current_user)
